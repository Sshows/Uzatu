export const metadata = {
  title: "SecureCourse Prototype",
  description:
    "Prototype of a protected learning platform with a public site, admin panel, and mobile student app."
};

export default function SecureCourseLayout({ children }) {
  return children;
}
